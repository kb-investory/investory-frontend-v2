# 백엔드 서버의 origin입니다. API 명세의 endpoint는 이 주소 뒤에 붙습니다.
# 예: http://localhost:8080 + /auth/logout
VITE_API_BASE_URL=http://localhost:8080

# API 실패 시 목데이터 Fallback 사용 여부 (기본값: true, 끄려면 false 지정)
# VITE_USE_MOCK_FALLBACK=false

